{
  "name": "discount-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "echo 'No build step required for production'",
    "start": "node index.js",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "matteobonollo",
  "private": true,
  "workspaces": [
    "extensions/*"
  ]
}
